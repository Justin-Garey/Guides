# CloudFront Distrubtions

When creating a CloudFront Distribution, it will enable the Web Application Firewall (WAF) by default which can lead to unnecessary charges. Be sure to disable it if it is not something you need.